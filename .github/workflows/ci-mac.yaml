name: ci-mac
on:   [push]
jobs:
    ci:
        runs-on: macos-15
        strategy:
            matrix:
                run-config:
                    - { scheme: 'GitHubUpdaterTest - Debug',   configuration: 'Debug',   project: 'GitHubUpdaterTest.xcodeproj', build: 1, analyze: 1, test: 0, info: 1, destination: 'platform=macOS' }
                    - { scheme: 'GitHubUpdaterTest - Release', configuration: 'Release', project: 'GitHubUpdaterTest.xcodeproj', build: 1, analyze: 1, test: 0, info: 1, destination: 'platform=macOS' }
        steps:
            
            - uses: actions/checkout@v6
              with:
                submodules: 'recursive'
                
            - uses: macmade/action-xcodebuild@v1.0.0
